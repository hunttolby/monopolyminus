const { colors } = require('./colors')
const { scalingObj } = require('./scalingObj')
const { typography } = require('./typography')
const { boxShadow } = require('./shadows')

module.exports = {
  colors,
  scalingObj,
  typography,
  boxShadow
}
